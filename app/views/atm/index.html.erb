<div class="atm-container">
	<div class="atm-screen">
		<!-- Left Panel - Menu -->
		<div class="form-panel">
			<!-- Header -->
			<div class="atm-header">
				<div class="bank-logo">SecureBank ATM</div>
				<h1 class="welcome-title">
					Welcome, <%= @card_presenter.customer_first_name %>
				</h1>
				<p class="welcome-subtitle">
					<strong><%= @account_presenter.account_type_display %></strong>
					Account: <%= @account_presenter.formatted_account_number %> • Balance:
					<%= @account_presenter.formatted_balance %>
				</p>
			</div>

			<!-- ATM Menu Options -->
			<div class="atm-menu">
				<div class="menu-section">
					<h3 class="menu-title">Select Transaction</h3>

					<div class="menu-options">
						<button
							class="menu-option"
							type="button"
						>
							<span class="option-icon">💰</span>
							<span class="option-text">
								<strong>Check Balance</strong>
								<small
									>View current account balance and recent transactions</small
								>
							</span>
						</button>

						<button
							class="menu-option"
							type="button"
						>
							<span class="option-icon">💳</span>
							<span class="option-text">
								<strong>Withdraw Cash</strong>
								<small>Take money from your account</small>
							</span>
						</button>

						<button
							class="menu-option"
							type="button"
						>
							<span class="option-icon">💵</span>
							<span class="option-text">
								<strong>Make a deposit</strong>
								<small>Put money in your account</small>
							</span>
						</button>

						<button
							class="menu-option"
							type="button"
						>
							<span class="option-icon">📄</span>
							<span class="option-text">
								<strong>Mini Statement</strong>
								<small>View recent transactions</small>
							</span>
						</button>
					</div>
				</div>

				<!-- Logout Button -->
				<%= link_to logout_path, method: :delete, class: "logout-button", data:
				{ confirm: "Are you sure you want to end your session?" } do %>
				<span>🚪</span> End Session <% end %>
			</div>
		</div>

		<%= render 'multi_card_panel' %>
	</div>
</div>

<style>
	.atm-menu {
		margin-top: 15px;
	}

	.menu-title {
		font-size: 18px;
		font-weight: 600;
		color: #2d3436;
		margin-bottom: 15px;
	}

	.menu-options {
		display: flex;
		flex-direction: column;
		gap: 10px;
		margin-bottom: 25px;
	}

	.menu-option {
		display: flex;
		align-items: center;
		gap: 14px;
		padding: 16px;
		background: #f8f9fa;
		border: 2px solid #e9ecef;
		border-radius: 12px;
		cursor: pointer;
		transition: all 0.3s ease;
		text-align: left;
		width: 100%;
	}

	.menu-option:hover {
		background: #ffffff;
		border-color: #74b9ff;
		transform: translateY(-2px);
		box-shadow: 0 8px 25px rgba(116, 185, 255, 0.15);
	}

	.option-icon {
		font-size: 20px;
		width: 35px;
		text-align: center;
	}

	.option-text strong {
		display: block;
		font-size: 15px;
		font-weight: 600;
		color: #2d3436;
		margin-bottom: 3px;
	}

	.option-text small {
		font-size: 13px;
		color: #636e72;
	}

	.logout-button {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 8px;
		padding: 14px;
		background: #ff7675;
		color: white;
		border-radius: 12px;
		text-decoration: none;
		font-weight: 600;
		transition: all 0.3s ease;
	}

	.logout-button:hover {
		background: #e17055;
		transform: translateY(-2px);
		box-shadow: 0 8px 25px rgba(255, 118, 117, 0.3);
		color: white;
		text-decoration: none;
	}

	.account-panel {
		background: linear-gradient(135deg, #2d3436 0%, #636e72 100%);
		padding: 30px;
	}

	.account-info {
		width: 100%;
		max-width: 320px;
	}

	/* Multi-Card Container */
	.cards-container {
		position: relative;
		width: 100%;
		min-height: 200px;
		margin-bottom: 25px;
		perspective: 1000px;
	}

	.card-wrapper {
		position: absolute;
		width: 100%;
		height: 200px;
		cursor: pointer;
		transition: transform 0.3s ease;
	}

	.card-wrapper:hover {
		transform: translateX(0px) translateY(-5px) !important;
		z-index: 1000 !important;
	}

	.card-flipper {
		position: relative;
		width: 100%;
		height: 100%;
		transform-style: preserve-3d;
		transition: transform 0.6s ease;
	}

	.card-flipper.flipped {
		transform: rotateY(180deg);
	}

	.card-front,
	.card-back {
		position: absolute;
		width: 100%;
		height: 100%;
		backface-visibility: hidden;
		border-radius: 16px;
		padding: 20px;
		box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
	}

	.card-front {
		background: linear-gradient(135deg, #74b9ff 0%, #0984e3 100%);
		color: white;
	}

	.card-back {
		background: linear-gradient(135deg, #636e72 0%, #2d3436 100%);
		color: white;
		transform: rotateY(180deg);
		display: flex;
		flex-direction: column;
		justify-content: space-between;
	}

	/* Current card indicator */
	.current-card-badge {
		position: absolute;
		top: 10px;
		right: 10px;
		background: rgba(0, 184, 148, 0.9);
		color: white;
		padding: 4px 8px;
		border-radius: 4px;
		font-size: 10px;
		font-weight: bold;
		letter-spacing: 0.5px;
	}

	/* Card back elements */
	.magnetic-stripe {
		width: 100%;
		height: 35px;
		background: linear-gradient(90deg, #2d3436 0%, #636e72 50%, #2d3436 100%);
		margin: 15px 0;
		border-radius: 2px;
	}

	.signature-panel {
		background: rgba(255, 255, 255, 0.9);
		color: #2d3436;
		padding: 10px;
		border-radius: 4px;
		margin: 10px 0;
	}

	.signature-label {
		font-size: 8px;
		font-weight: bold;
		margin-bottom: 5px;
	}

	.signature-line {
		height: 20px;
		border-bottom: 1px solid #636e72;
		font-style: italic;
		display: flex;
		align-items: flex-end;
		font-size: 12px;
		color: #2d3436;
	}

	.card-info {
		display: flex;
		justify-content: space-between;
		margin-top: 10px;
	}

	.card-info-item {
		text-align: center;
		flex: 1;
	}

	.card-info-item small {
		display: block;
		font-size: 8px;
		opacity: 0.8;
		margin-bottom: 2px;
	}

	.card-info-item span {
		font-size: 11px;
		font-weight: 600;
	}

	.card-network-logo {
		text-align: right;
		font-weight: bold;
		font-size: 12px;
		opacity: 0.8;
	}

	.cvc-display {
		font-family: monospace;
		letter-spacing: 1px;
	}

	.cards-instruction {
		text-align: center;
		color: rgba(255, 255, 255, 0.8);
		margin-top: 15px;
	}

	.cards-instruction small {
		font-size: 12px;
	}

	.card-count-indicator {
		margin-top: 8px;
		padding: 4px 8px;
		background: rgba(255, 255, 255, 0.1);
		border-radius: 12px;
		backdrop-filter: blur(10px);
		display: inline-block;
	}

	.card-count-indicator small {
		font-size: 10px;
		font-weight: 600;
		letter-spacing: 0.5px;
	}	/* Vertical Cards Display */
	.vertical-cards-container {
		display: flex;
		flex-direction: column;
		gap: 20px;
		width: 100%;
		margin-bottom: 20px;
	}

	.vertical-card {
		position: relative;
		width: 95%;
		height: 180px;
		cursor: pointer;
		transition: all 0.3s ease;
		perspective: 1000px;
		margin: 0 auto;
	}

	/* Shadow card behind each card */
	.vertical-card::before {
		content: '';
		position: absolute;
		top: 4px;
		left: 4px;
		right: -4px;
		bottom: -4px;
		background: rgba(0, 0, 0, 0.1);
		border-radius: 16px;
		z-index: -1;
		transition: all 0.3s ease;
	}

	.vertical-card:hover {
		transform: translateY(-3px) scale(1.01);
		filter: drop-shadow(0 8px 20px rgba(0, 0, 0, 0.2));
	}

	.vertical-card:hover::before {
		transform: translateY(2px);
		background: rgba(0, 0, 0, 0.15);
	}

	.vertical-card:active {
		transform: translateY(-1px) scale(0.99);
		transition: all 0.1s ease;
	}

	.card-flipper {
		position: relative;
		width: 100%;
		height: 100%;
		transform-style: preserve-3d;
		transition: transform 0.6s ease;
	}

	.card-flipper.flipped {
		transform: rotateY(180deg);
	}

	.card-front,
	.card-back {
		position: absolute;
		width: 100%;
		height: 100%;
		backface-visibility: hidden;
		border-radius: 16px;
		overflow: hidden;
	}

	.card-back {
		transform: rotateY(180deg);
		background: linear-gradient(135deg, #636e72 0%, #2d3436 100%);
		color: white;
		padding: 15px;
		display: flex;
		flex-direction: column;
		justify-content: space-between;
	}

	.magnetic-stripe {
		width: 100%;
		height: 35px;
		background: linear-gradient(90deg, #2d3436 0%, #636e72 50%, #2d3436 100%);
		margin: 10px 0;
		border-radius: 2px;
		box-shadow: inset 0 2px 4px rgba(0, 0, 0, 0.3);
	}

	.signature-panel {
		background: rgba(255, 255, 255, 0.95);
		color: #2d3436;
		padding: 8px;
		border-radius: 4px;
		margin: 10px 0;
	}

	.signature-label {
		font-size: 8px;
		font-weight: bold;
		margin-bottom: 5px;
		letter-spacing: 0.5px;
	}

	.signature-line {
		height: 18px;
		border-bottom: 1px solid #636e72;
		font-style: italic;
		font-size: 11px;
		display: flex;
		align-items: flex-end;
	}

	.card-info {
		display: flex;
		justify-content: space-between;
		margin-top: 15px;
		gap: 10px;
	}

	.card-info-item {
		text-align: center;
		flex: 1;
	}

	.card-info-item small {
		display: block;
		font-size: 8px;
		opacity: 0.8;
		margin-bottom: 2px;
		font-weight: bold;
		letter-spacing: 0.5px;
	}

	.card-info-item span {
		font-size: 11px;
		font-weight: 600;
	}

	.card-network-logo {
		text-align: right;
		font-weight: bold;
		font-size: 12px;
		opacity: 0.9;
		margin-top: 10px;
		letter-spacing: 1px;
	}

	.cvc-display {
		font-family: monospace;
		letter-spacing: 2px;
		background: rgba(255, 255, 255, 0.1);
		padding: 2px 6px;
		border-radius: 3px;
	}

	.current-card-badge {
		position: absolute;
		top: 15px;
		right: 15px;
		background: linear-gradient(45deg, #00b894, #00a085);
		color: white;
		padding: 4px 10px;
		border-radius: 12px;
		font-size: 9px;
		font-weight: bold;
		letter-spacing: 0.5px;
		text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
		box-shadow: 0 2px 8px rgba(0, 184, 148, 0.3);
		z-index: 10;
	}

	.account-card {
		background: linear-gradient(135deg, #74b9ff 0%, #0984e3 100%);
		border-radius: 16px;
		padding: 25px;
		color: white;
		margin-bottom: 25px;
		box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
	}

	.card-header {
		display: flex;
		justify-content: space-between;
		align-items: center;
		margin-bottom: 25px;
	}

	.card-chip {
		width: 35px;
		height: 25px;
		background: #fdcb6e;
		border-radius: 6px;
		position: relative;
	}

	.card-chip::before {
		content: "";
		position: absolute;
		top: 5px;
		left: 5px;
		right: 5px;
		bottom: 5px;
		background: linear-gradient(45deg, #fdcb6e, #e17055);
		border-radius: 2px;
	}

	.card-logo {
		font-size: 16px;
		font-weight: bold;
		letter-spacing: 2px;
	}

	.card-number {
		font-size: 20px;
		font-weight: 600;
		letter-spacing: 3px;
		margin-bottom: 18px;
		font-family: "Courier New", monospace;
	}

	.card-details {
		display: flex;
		justify-content: space-between;
	}

	.card-details small {
		display: block;
		font-size: 9px;
		margin-bottom: 3px;
		opacity: 0.8;
	}

	.account-summary {
		background: rgba(255, 255, 255, 0.1);
		backdrop-filter: blur(10px);
		border-radius: 16px;
		padding: 20px;
		color: white;
	}

	.account-summary h4 {
		margin-bottom: 15px;
		font-size: 16px;
	}

	.summary-item {
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: 10px 0;
		border-bottom: 1px solid rgba(255, 255, 255, 0.1);
	}

	.summary-item:last-child {
		border-bottom: none;
	}

	.status-active {
		color: #00b894;
		font-weight: 600;
	}

	.status-blocked,
	.status-expired {
		color: #ff7675;
		font-weight: 600;
	}

	.status-unknown {
		color: #fdcb6e;
		font-weight: 600;
	}

	@media (max-width: 1024px) {
		.account-info {
			max-width: 280px;
		}

		.account-card {
			padding: 20px;
		}

		.card-number {
			font-size: 18px;
		}
	}

	@media (max-width: 768px) {
		.account-panel {
			display: none;
		}

		.menu-option {
			padding: 14px;
		}

		.option-icon {
			font-size: 18px;
			width: 30px;
		}

		.option-text strong {
			font-size: 14px;
		}

		.option-text small {
			font-size: 12px;
		}
	}
</style>
